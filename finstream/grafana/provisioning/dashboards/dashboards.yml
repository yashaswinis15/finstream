apiVersion: 1

providers:
  - name: finstream-file-provisioning
    orgId: 1
    folder: FinStream
    type: file
    disableDeletion: true
    allowUiUpdates: true
    updateIntervalSeconds: 30

    options:
      path: /var/lib/grafana/dashboards